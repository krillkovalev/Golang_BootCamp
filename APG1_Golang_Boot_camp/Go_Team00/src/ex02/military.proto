syntax = "proto3";

package military;

option go_package = "./military";

service DeviceService {
  rpc StreamData (ConnectionRequest) returns (stream DeviceData);
}

message ConnectionRequest {
  string client_id = 1;
}

message DeviceData {
  string session_id = 1;
  double frequency = 2;
  string timestamp = 3;
}